{
  "name": "apic-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for APIC credit system",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-comprehensive.js --network base-sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-comprehensive.js --network base",
    "test:purchase": "hardhat run scripts/test-purchase.js --network base-sepolia",
    "verify": "hardhat verify --network base-sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "solc": "^0.8.20"
  }
}
